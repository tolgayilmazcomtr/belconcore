<!DOCTYPE html>
<html lang="tr">
<head>
<meta charset="UTF-8">
<meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
<title>Teklif</title>
<style>
@page {
    margin-top: 6mm;
    margin-right: 3mm;
    margin-bottom: 3mm;
    margin-left: 3mm;
}
* { margin:0; padding:0; }
html {
    font-family: 'DejaVu Sans', sans-serif;
    font-size: 8pt;
    color: #1a1a1a;
    background: #fff;
    line-height: 1.4;
}
body {
    margin: 6mm 3mm 3mm 3mm;
}
.page { width:100%; }

/* ── HEADER ── */
.hdr { width:100%; border-collapse:collapse; }
.hdr td { vertical-align:middle; padding:0; }
.logo-img { max-height:40px; max-width:120px; }
.co-name  { font-size:13pt; font-weight:700; color:#111; }
.co-sub   { font-size:5.5pt; font-weight:700; color:#C8102E; letter-spacing:0.14em; text-transform:uppercase; margin-top:2px; }
.hdr-r    { text-align:right; vertical-align:top; }
.doc-lbl  { font-size:5.5pt; font-weight:700; color:#999; letter-spacing:0.14em; text-transform:uppercase; }
.doc-no   { font-size:12pt; font-weight:700; color:#111; margin:2px 0 3px 0; }
.s-badge  { display:inline-block; font-size:5.5pt; font-weight:700; letter-spacing:0.07em; text-transform:uppercase; padding:2px 7px; border:1px solid; }
.sd { color:#666; border-color:#ccc; background:#f5f5f5; }
.ss { color:#1a56db; border-color:#a4cafe; background:#eff6ff; }
.sa { color:#057a55; border-color:#6ee7b7; background:#ecfdf5; }
.sr { color:#b91c1c; border-color:#fca5a5; background:#fef2f2; }

.rule { border:none; border-top:2px solid #C8102E; margin:3mm 0; }

/* ── META ── */
.meta { width:100%; border-collapse:collapse; margin-bottom:3mm; }
.meta > tr > td { vertical-align:top; padding:0; }
.ml { width:84mm; padding-right:5mm; border-right:1px solid #e8e8e8; }
.mr { width:84mm; padding-left:5mm; }
.sec  { font-size:5pt; font-weight:700; color:#999; letter-spacing:0.14em; text-transform:uppercase; margin-bottom:2px; }
.kv   { width:100%; border-collapse:collapse; }
.kv td{ font-size:7pt; padding:0.4px 0; vertical-align:top; }
.k    { color:#888; width:56px; }
.v    { color:#111; font-weight:600; }
.vred { color:#C8102E; font-weight:700; }

/* ── SECTION HEAD ── */
.sh {
    font-size:5.5pt; font-weight:700; color:#888; letter-spacing:0.14em; text-transform:uppercase;
    border-bottom:1px solid #e4e4e4; padding-bottom:1.5px; margin:3mm 0 2mm 0;
}

/* ── FİYAT TABLOSU ── */
.pt { width:100%; border-collapse:collapse; }
.pt th { background:#111; color:#fff; font-size:6pt; font-weight:700; text-transform:uppercase; letter-spacing:0.07em; padding:4pt 8pt; text-align:left; }
.pt th.r { text-align:right; }
.pt td   { padding:3.5pt 8pt; font-size:8pt; color:#333; border-bottom:1px solid #f0f0f0; }
.pt td.r { text-align:right; }
.pt .alt td  { background:#fafafa; }
.pt .tot     { }
.pt .tot td  { padding:4pt 8pt; font-size:9.5pt; font-weight:700; color:#C8102E; border-top:1.5px solid #ddd; border-bottom:none; }
.pt .tot td.r{ text-align:right; }
.disc { color:#b91c1c; }

/* ── ÖDEME PLANI ── */
.pnote { font-size:6.5pt; color:#777; padding:2mm 0; margin-bottom:2.5mm; border-bottom:1px solid #f0f0f0; }
.ot { width:100%; border-collapse:collapse; }
.ot th { background:#1f2937; color:#fff; font-size:5.5pt; font-weight:700; text-transform:uppercase; letter-spacing:0.06em; padding:3.5pt 6pt; text-align:left; }
.ot th.r { text-align:right; }
.ot td   { padding:3pt 6pt; font-size:7pt; color:#333; border-bottom:1px solid #f0f0f0; }
.ot td.r { text-align:right; }
.ot .alt td  { background:#fafafa; }
.ot tfoot td { background:#f5f5f5; font-weight:700; font-size:7pt; color:#111; border-top:1.5px solid #ddd; padding:3.5pt 6pt; }
.ot tfoot td.r { text-align:right; }
.inf { font-size:5pt; color:#b45309; }

/* ── METİN ── */
.txt { font-size:7.5pt; color:#444; line-height:1.55; border-left:2px solid #ddd; padding:0 0 0 3mm; margin-bottom:2.5mm; }

/* ── YASAL ── */
.legal { font-size:5.5pt; color:#bbb; line-height:1.45; border-top:1px solid #f0f0f0; padding-top:1.5mm; margin-top:2.5mm; }

/* ── İMZA ── */
.sig { width:100%; border-collapse:collapse; margin-top:3mm; }
.sig td { width:33%; vertical-align:bottom; padding:0 3mm 0 0; }
.sig td:last-child { padding-right:0; }
.sline { border-top:1px solid #ccc; padding-top:3px; margin-top:8mm; }
.sname { font-size:7pt; font-weight:700; color:#333; }
.srole { font-size:5.5pt; color:#bbb; margin-top:1px; }

/* ── FOOTER ── */
.ft { width:100%; border-collapse:collapse; border-top:1px solid #e0e0e0; margin-top:3mm; padding-top:0; }
.ft td { padding-top:1.5mm; font-size:5.5pt; color:#bbb; line-height:1.4; vertical-align:middle; }
.fb { font-size:5.5pt; font-weight:700; color:#111; display:inline; }
.fr { text-align:right; }
.fn { color:#C8102E; font-weight:700; font-size:5.5pt; }
</style>
</head>
<body>
<div class="page">

@php
    $proj     = $offer->project;
    $coName   = ($proj && $proj->company_name) ? $proj->company_name : ($proj->name ?? 'Belcon');
    $logoPath = ($proj && $proj->logo_path) ? storage_path('app/public/' . $proj->logo_path) : null;
    $hasLogo  = $logoPath && file_exists($logoPath);
    $logoSrc  = null;
    if ($hasLogo) {
        $mime = mime_content_type($logoPath);
        $logoSrc = 'data:' . $mime . ';base64,' . base64_encode(file_get_contents($logoPath));
    }

    $sm = ['draft'=>['TASLAK','sd'],'sent'=>['GONDER&#304;LD&#304;','ss'],'accepted'=>['KABUL ED&#304;LD&#304;','sa'],'rejected'=>['REDDEDD&#304;LD&#304;','sr']];
    $sLabel = $sm[$offer->status][0] ?? strtoupper($offer->status);
    $sClass = $sm[$offer->status][1] ?? 'sd';

    $c     = $offer->customer;
    $u     = $offer->unit;
    $cName = '';
    if ($c) {
        $cName = ($c->type === 'corporate') ? $c->company_name : trim($c->first_name . ' ' . $c->last_name);
    }
    $unitLabel = '';
    $alanStr   = '';
    $katStr    = '';
    if ($u) {
        $bn        = isset($u->block) ? ($u->block->name ?? '') : '';
        $unitLabel = ($bn ? $bn . ' Blok - ' : '') . 'No: ' . $u->unit_no;
        $ap = [];
        if ($u->net_area)   $ap[] = $u->net_area . ' m2 net';
        if ($u->gross_area) $ap[] = $u->gross_area . ' m2 brut';
        $alanStr = implode(' / ', $ap);
        $kr = ($u->floor_no !== null && $u->floor_no !== '') ? trim((string)$u->floor_no) : '';
        if ($kr !== '') {
            $katStr = (mb_stripos($kr, 'kat') !== false) ? $kr : $kr . '. Kat';
        }
    }
    $typeLabels = [
        'downpayment_only'   => 'Tam Pe&#351;in',
        'lump_sum'           => 'Tek Seferlik',
        'equal_installments' => 'E&#351;it Taksit',
        'fixed_amount'       => 'Sabit Tutar',
        'custom'             => 'Ozel Plan',
    ];
@endphp

{{-- HEADER --}}
<table class="hdr">
<tr>
  <td style="width:55%">
    @if($hasLogo && $logoSrc)
      <img src="{{ $logoSrc }}" class="logo-img" alt="{{ $coName }}">
    @else
      <div class="co-name">{{ $coName }}</div>
      <div class="co-sub">Gayrimenkul &amp; Insaat</div>
    @endif
  </td>
  <td class="hdr-r" style="width:45%">
    <div class="doc-lbl">Satis Teklifi</div>
    <div class="doc-no">{{ $offer->offer_no }}</div>
    <span class="s-badge {{ $sClass }}">{!! $sLabel !!}</span>
  </td>
</tr>
</table>
<hr class="rule">

{{-- META --}}
<table class="meta">
<tr>
  <td class="ml">
    <div class="sec">Musteri</div>
    @if($cName)
      <table class="kv"><tr><td class="k">Ad / Unvan</td><td class="v">{{ $cName }}</td></tr></table>
    @endif
    @if($c && $c->phone)
      <table class="kv"><tr><td class="k">Telefon</td><td class="v">{{ $c->phone }}</td></tr></table>
    @endif
    @if($c && $c->email)
      <table class="kv"><tr><td class="k">E-posta</td><td class="v">{{ $c->email }}</td></tr></table>
    @endif
    @if($c && $c->address)
      <table class="kv"><tr><td class="k">Adres</td><td class="v">{{ $c->address }}{{ $c->city ? ', '.$c->city : '' }}</td></tr></table>
    @endif
    @if($c && $c->type === 'corporate' && $c->tax_number)
      <table class="kv"><tr><td class="k">Vergi No</td><td class="v">{{ $c->tax_number }}{{ $c->tax_office ? ' / '.$c->tax_office : '' }}</td></tr></table>
    @endif
  </td>
  <td class="mr">
    <div class="sec">Teklif &amp; Gayrimenkul</div>
    @if($unitLabel)
      <table class="kv"><tr><td class="k">Konut</td><td class="v">{{ $unitLabel }}</td></tr></table>
    @endif
    @if($u && $u->unit_type)
      <table class="kv"><tr><td class="k">Tip</td><td class="v">{{ $u->unit_type }}</td></tr></table>
    @endif
    @if($alanStr)
      <table class="kv"><tr><td class="k">Alan</td><td class="v">{{ $alanStr }}</td></tr></table>
    @endif
    @if($katStr)
      <table class="kv"><tr><td class="k">Kat</td><td class="v">{{ $katStr }}</td></tr></table>
    @endif
    <table class="kv"><tr><td class="k">Tarih</td><td class="v">{{ $offer->created_at ? $offer->created_at->format('d.m.Y') : now()->format('d.m.Y') }}</td></tr></table>
    @if($offer->valid_until)
      <table class="kv"><tr><td class="k">Gecerlilik</td><td class="vred">{{ \Carbon\Carbon::parse($offer->valid_until)->format('d.m.Y') }}</td></tr></table>
    @endif
    @if($offer->creator)
      <table class="kv"><tr><td class="k">Hazirlayan</td><td class="v">{{ $offer->creator->name }}</td></tr></table>
    @endif
  </td>
</tr>
</table>

{{-- FİYATLANDIRMA --}}
<div class="sh">Fiyatlandirma</div>
<table class="pt">
  <thead>
    <tr>
      <th style="width:65%">Aciklama</th>
      <th class="r" style="width:35%">Tutar</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Liste / Baz Satis Fiyati</td>
      <td class="r">{{ number_format($offer->base_price,0,',','.') }} TL</td>
    </tr>
    @if($offer->discount_amount > 0)
    <tr class="alt">
      <td class="disc">Ozel Indirim</td>
      <td class="r disc">- {{ number_format($offer->discount_amount,0,',','.') }} TL</td>
    </tr>
    @endif
    <tr class="tot">
      <td>Net Teklif Fiyati</td>
      <td class="r">{{ number_format($offer->final_price,0,',','.') }} TL</td>
    </tr>
  </tbody>
</table>

{{-- ÖDEME PLANI --}}
@if($offer->payment_plan)
@php
    $planData = null;
    $dec = json_decode($offer->payment_plan, true);
    if (is_array($dec) && !empty($dec['items'])) $planData = $dec;
@endphp
<div class="sh">Odeme Plani</div>
@if($planData)
  @php
    $annRate = round((pow(1 + $planData['monthly_inflation_rate']/100, 12)-1)*100, 1);
    $tLabel  = $typeLabels[$planData['type']] ?? '-';
  @endphp
  <div class="pnote">
    Plan: <strong>{!! $tLabel !!}</strong>
    &nbsp;&middot;&nbsp; Aylik enflasyon: <strong>%{{ $planData['monthly_inflation_rate'] }}</strong> (~Yillik %{{ $annRate }})
    &nbsp;&middot;&nbsp; Baz: {{ \Carbon\Carbon::parse($planData['offer_date'])->format('d.m.Y') }}
  </div>
  @php
    $allItems  = array_values($planData['items']);
    $itemCount = count($allItems);
    $splitMode = $itemCount > 12;
    $col1      = $splitMode ? array_slice($allItems, 0, 12) : $allItems;
    $col2      = $splitMode ? array_slice($allItems, 12)    : [];
  @endphp

  @if(!$splitMode)
    {{-- TEK TABLO: 12 veya daha az kalem --}}
    <table class="ot">
      <thead><tr>
        <th style="width:6%">#</th>
        <th style="width:28%">Aciklama</th>
        <th style="width:20%">Odeme Tarihi</th>
        <th class="r" style="width:22%">Bugunun Degeri</th>
        <th class="r" style="width:24%">Odenecek Tutar</th>
      </tr></thead>
      <tbody>
        @foreach($col1 as $i => $item)
        <tr class="{{ $i%2===1?'alt':'' }}">
          <td style="color:#bbb">{{ $item['no'] }}</td>
          <td><strong>{{ $item['description'] }}</strong></td>
          <td>{{ $item['date'] }}</td>
          <td class="r" style="color:#aaa">{{ number_format($item['real_amount'],0,',','.') }} TL</td>
          <td class="r">
            <strong>{{ number_format($item['nominal_amount'],0,',','.') }} TL</strong>
            @if($item['month_offset']>0)
              @php $pct=round(($item['nominal_amount']/max($item['real_amount'],1)-1)*100,1); @endphp
              <span class="inf"> +%{{ $pct }}</span>
            @endif
          </td>
        </tr>
        @endforeach
      </tbody>
      <tfoot><tr>
        <td colspan="3">Toplam</td>
        <td class="r" style="color:#888">{{ number_format($planData['total_real'],0,',','.') }} TL</td>
        <td class="r">{{ number_format($planData['total_nominal'],0,',','.') }} TL</td>
      </tr></tfoot>
    </table>
  @else
    {{-- YANYANA 2 TABLO: 12+ kalem --}}
    <table style="width:100%; border-collapse:collapse;">
      <tr>
        {{-- Sol tablo: 1-12 --}}
        <td style="width:49%; vertical-align:top; padding-right:2mm;">
          <table class="ot" style="width:100%">
            <thead><tr>
              <th style="width:7%">#</th>
              <th style="width:33%">Aciklama</th>
              <th style="width:24%">Tarih</th>
              <th class="r" style="width:36%">Tutar</th>
            </tr></thead>
            <tbody>
              @foreach($col1 as $i => $item)
              <tr class="{{ $i%2===1?'alt':'' }}">
                <td style="color:#bbb">{{ $item['no'] }}</td>
                <td><strong>{{ $item['description'] }}</strong></td>
                <td>{{ $item['date'] }}</td>
                <td class="r">
                  <strong>{{ number_format($item['nominal_amount'],0,',','.') }} TL</strong>
                  @if($item['month_offset']>0)
                    @php $pct=round(($item['nominal_amount']/max($item['real_amount'],1)-1)*100,1); @endphp
                    <span class="inf"> +%{{ $pct }}</span>
                  @endif
                </td>
              </tr>
              @endforeach
            </tbody>
          </table>
        </td>
        {{-- Orta ince ayraç --}}
        <td style="width:2%; vertical-align:top; border-left:1px solid #e4e4e4;"></td>
        {{-- Sağ tablo: 13+ --}}
        <td style="width:49%; vertical-align:top; padding-left:2mm;">
          <table class="ot" style="width:100%">
            <thead><tr>
              <th style="width:7%">#</th>
              <th style="width:33%">Aciklama</th>
              <th style="width:24%">Tarih</th>
              <th class="r" style="width:36%">Tutar</th>
            </tr></thead>
            <tbody>
              @foreach($col2 as $i => $item)
              <tr class="{{ $i%2===1?'alt':'' }}">
                <td style="color:#bbb">{{ $item['no'] }}</td>
                <td><strong>{{ $item['description'] }}</strong></td>
                <td>{{ $item['date'] }}</td>
                <td class="r">
                  <strong>{{ number_format($item['nominal_amount'],0,',','.') }} TL</strong>
                  @if($item['month_offset']>0)
                    @php $pct=round(($item['nominal_amount']/max($item['real_amount'],1)-1)*100,1); @endphp
                    <span class="inf"> +%{{ $pct }}</span>
                  @endif
                </td>
              </tr>
              @endforeach
            </tbody>
          </table>
        </td>
      </tr>
      {{-- Toplam satırı - tam genişlik --}}
      <tr>
        <td colspan="3" style="padding-top:2mm; border-top:1.5px solid #ddd;">
          <table style="width:100%; border-collapse:collapse;">
            <tr>
              <td style="font-size:7pt; font-weight:700; color:#111; padding:3pt 6pt; background:#f5f5f5;">Toplam ({{ $itemCount }} kalem)</td>
              <td style="font-size:7pt; color:#888; text-align:right; padding:3pt 6pt; background:#f5f5f5;">{{ number_format($planData['total_real'],0,',','.') }} TL bugunku deger</td>
              <td style="font-size:7pt; font-weight:700; color:#111; text-align:right; padding:3pt 6pt; background:#f5f5f5; width:28%;">{{ number_format($planData['total_nominal'],0,',','.') }} TL</td>
            </tr>
          </table>
        </td>
      </tr>
    </table>
  @endif
@else
  <div class="txt">{{ $offer->payment_plan }}</div>
@endif
@endif

{{-- NOTLAR --}}
@if($offer->notes)
<div class="sh">Notlar</div>
<div class="txt">{{ $offer->notes }}</div>
@endif

{{-- YASAL --}}
<div class="legal">
  Bu belge bilgilendirme amacli olup hukuki baglayicilik tasimaz.
  Satis; gecerlilik suresi icinde taraflarca imzalanmis satis sozlesmesi ile kesinlesir.
  Fiyatlar KDV haric Turk Lirasi cinsindendir.
</div>

{{-- İMZA --}}
<table class="sig">
<tr>
  <td><div class="sline"><div class="sname">Musteri</div><div class="srole">Ad Soyad / Imza</div></div></td>
  <td><div class="sline"><div class="sname">Yetkili Temsilci</div><div class="srole">Ad Soyad / Imza / Kase</div></div></td>
  <td><div class="sline"><div class="sname">Tarih</div><div class="srole">GG / AA / YYYY</div></div></td>
</tr>
</table>

{{-- FOOTER --}}
@php
    $footerParts = [];
    if ($proj) {
        $footerParts[] = '<strong>' . e($proj->company_name ?? $proj->name) . '</strong>';
        if ($proj->company_address) $footerParts[] = e($proj->company_address);
        if ($proj->company_phone)   $footerParts[] = 'Tel: ' . e($proj->company_phone);
        if ($proj->company_email)   $footerParts[] = e($proj->company_email);
        if ($proj->tax_number)      $footerParts[] = 'VKN: ' . e($proj->tax_number) . ($proj->tax_office ? ' / ' . e($proj->tax_office) : '');
    }
    $footerLeft = implode(' &nbsp;|&nbsp; ', $footerParts);
@endphp
<table class="ft">
<tr>
  <td style="width:68%">{!! $footerLeft !!}</td>
  <td class="fr" style="width:32%">
    <span class="fn">{{ $offer->offer_no }}</span>
    &nbsp;&middot;&nbsp; {{ now()->format('d.m.Y') }}
    &nbsp;&middot;&nbsp; Elektronik ortamda uretilmistir.
  </td>
</tr>
</table>

</div>
</body>
</html>
